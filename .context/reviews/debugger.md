# debugger Review (Cycle 52)

**Date:** 2026-04-29
**HEAD:** 331c6f5

## Latent bug surface scan

### F52-01 (paired with code-reviewer / verifier / tracer): year `int("2023.0")` ValueError → silent drop — LOW / MEDIUM

- **File:** `pixelpitch.py:366-372`
- **Failure mode:** Excel hand-edit of `dist/camera-data.csv` rewrites
  integer years as floats; `int("2023.0")` raises; the except branch
  drops the year. After CI re-renders, the year column is blank for
  every edited row. Silent data loss.
- **Severity:** LOW (no current trigger from internal write path)
- **Confidence:** MEDIUM (clear failure, narrow trigger)

### F52-D-02 (low confidence, related): record_id parsing has the same int-vs-float vulnerability

- **File:** `pixelpitch.py:319` — `record_id = int(values[0]) if values[0] else None`
- **Detail:** If Excel rewrites the id column as `"5.0"`, `int(...)`
  raises ValueError, caught by the broad except at line 390 — the row
  is logged and SKIPPED entirely. This is more disruptive than the year
  case (entire row lost vs. one column).
- **Mitigation:** record_id is regenerated by `merge_camera_data` (line
  559-560), so the impact is recoverable: the next render restores ids
  on every camera. The skip happens once per affected row, but render
  still completes.
- **Severity:** LOW (recoverable; same Excel-hand-edit class as F52-01)
- **Confidence:** MEDIUM
- **Status:** flag for the F52-01 fix to handle in the same plan if low
  cost; otherwise defer.

## No high-severity latent bugs identified this cycle.
