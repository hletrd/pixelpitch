# Plan C14-01: openMVG DSLR Classification, BOM Defense, CineD Dead Code, Mutable Alias

**Status:** completed
**Priority:** P1 (2 MEDIUM correctness bugs, 3 LOW robustness fixes)
**Findings addressed:** C14-01, C14-02, C14-03, C14-04, C14-05

## Problem

1. **C14-01 (MEDIUM):** openMVG classifies all interchangeable-lens cameras as "mirrorless" (`size[0] >= 20 → mirrorless`). DSLRs like Canon EOS 5D get `category="mirrorless"` instead of `"dslr"`, causing duplicate entries on the All Cameras page when the same camera exists in Geizhals data with `category="dslr"`.

2. **C14-02 (MEDIUM):** UTF-8 BOM in `camera-data.csv` causes `parse_existing_csv` to produce 0 rows. The BOM makes `header[0] = "﻿id"` instead of `"id"`, breaking schema detection. Common trigger: saving CSV from Excel.

3. **C14-03 (LOW):** CineD `FORMAT_TO_MM` has 3 unreachable entries: `"super35"`, `"1 inch"`, `"2/3-inch"`. The regex in `_parse_camera_page` doesn't capture these string patterns. Either extend the regex or remove the dead entries.

4. **C14-04 (LOW):** `gsmarena.PHONE_TYPE_SIZE` is a mutable alias of `pixelpitch.TYPE_SIZE`. Previous fix (C9-06) only added a comment. Should use `MappingProxyType` or `dict()` copy to enforce read-only access.

5. **C14-05 (LOW):** openMVG module docstring doesn't document the category heuristic limitation (DSLR misclassification). Developers reading the docstring won't understand why duplicates may appear.

## Implementation Steps

### Step 1: Fix openMVG DSLR classification (C14-01, C14-05)
- [x] Add a DSLR name-pattern heuristic to `sources/openmvg.py`
- [x] Pattern: names matching Canon EOS [0-9]D, Nikon D[0-9], Pentax K-[0-9], Sigma SD, etc.
- [x] Update the docstring to document the category heuristic limitation
- [x] Update existing test to assert `category="dslr"` for Canon EOS 5D
- [x] Add test cases for other DSLR name patterns
- [x] Commit: 55790c2

### Step 2: Add BOM defense to parse_existing_csv (C14-02)
- [x] Strip UTF-8 BOM at the entry point of `parse_existing_csv` in `pixelpitch.py`
- [x] Add test for BOM-prefixed CSV content
- [x] Commit: 5cf5cab

### Step 3: Fix CineD unreachable FORMAT_TO_MM entries (C14-03)
- [x] Extend regex in `_parse_camera_page` to capture missing variants: `Super[- ]?35`, `1[- ]inch`, `2/3[- ]inch`
- [x] Update `test_cined_format_coverage` to verify all FORMAT_TO_MM keys are reachable
- [x] Commit: b71ff6e

### Step 4: Make PHONE_TYPE_SIZE truly read-only (C14-04)
- [x] Change `PHONE_TYPE_SIZE = SENSOR_TYPE_SIZE` to `dict(SENSOR_TYPE_SIZE)`
- [x] Update the existing comment
- [x] Commit: 7555feb

### Step 5: Verify all fixes
- [x] Run gate tests — ALL PASS
- [x] Verify no regressions
- [x] Push all commits — DONE

## Exit Criteria
- openMVG DSLRs classified as "dslr" — DONE (verified by test)
- BOM-prefixed CSV parsed correctly — DONE (verified by test)
- All FORMAT_TO_MM entries reachable by regex — DONE (verified by test)
- PHONE_TYPE_SIZE is a read-only copy — DONE (verified by code)
- All gate tests pass — DONE
